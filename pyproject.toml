[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mess"
version = "0.1.0"
description = "MESS & AdaMESS: Maximum Entropy Snapshot Sampling for memory-efficient reduced-basis generation"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Steven Stroka", email = "stroka@uni-wuppertal.de"}]
keywords = ["model order reduction", "reduced basis", "snapshot sampling", "POD", "scientific computing", "electromagnetics"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering",
]

# Specify the required Python version
requires-python = ">=3.11"

# Core dependencies
dependencies = [
    "matplotlib",
    "numpy",
    "scipy",
    "h5py"
]

[tool.hatch.build.targets.wheel]
packages = ["src/mess"]

# Configure the source layout by specifying the package directory
[tool.hatch.metadata]
root = "src/mess"
